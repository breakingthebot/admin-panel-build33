name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    uses: breakingthebot/shared-workflows/.github/workflows/node-ci.yml@main
    with:
      node-version: '20'
